spring:

  liquibase:
    default-schema: 'app'

  kafka:
    bootstrap-servers: "${spring.embedded.kafka.brokers}"

application:
  feign:
    clients:
      user-service:
        url: "http://localhost:${wiremock.server.port}"